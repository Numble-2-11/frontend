import styled from "styled-components";

export const SignupDes = styled.div`
  font-size: ${(props) => props.fontSize || "1.8rem"};
  color: #bfbfbf;
  text-align: ${(props) => props.fontSize || "left"};
  margin: ${(props) => props.margin || "3rem 0"};
`;
